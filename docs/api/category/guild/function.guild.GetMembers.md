# guild.GetMembers

Возвращает список членов гильдии.

## Описание

```lua
guild.GetMembers(): table
```

Функция возвращает список идентификаторов членов гильдии.

## Список параметров

Параметры отсутствуют.

## Возвращаемые значения

Возвращает `table` - индексированная с 0 таблица идентификаторов (`ObjectId`) членов гильдии.

## Примеры

### Получение информации о члене гильдии

```lua
local members = guild.GetMembers()
if members[ 0 ] then
    local memberInfo = guild.GetMemberInfo( members[ 0 ] )
    if memberInfo then
        local name = memberInfo.name
    end
end
```

::: info Описание примера
В примере сначала извлекается таблица идентификаторов членов гильдии. Затем проверяется наличие первого элемента (с индексом 0). Если элемент существует, извлекается подробная информация о члене гильдии с помощью `guild.GetMemberInfo`, и при успешном получении данных извлекается имя персонажа.
:::

## Смотрите также

- [guild.GetMemberInfo](function.guild.GetMemberInfo)