# itemLib.GetSpell
Возвращает идентификатор заклинания, прикреплённого к предмету.

## Описание
```lua
itemLib.GetSpell( itemId: ObjectId ): SpellId | nil
```
Функция извлекает и возвращает идентификатор заклинания, прикреплённого к указанному предмету. В случае отсутствия заклинания у предмета возвращается `nil`.

## Список параметров
- **`itemId`** (`ObjectId`) - Уникальный идентификатор предмета.

## Возвращаемые значения
Возвращает `SpellId` | `nil` - идентификатор заклинания, если оно присутствует у предмета, или `nil`, если заклинание отсутствует.

## Примеры
### Получение названия заклинания предмета
```lua
local spellId = itemLib.GetSpell( itemId )
local spellInfo = spellId and spellLib.GetDescription( spellId )
local spellName = spellInfo and spellInfo.name
```
::: info Описание примера
В примере сначала извлекается идентификатор заклинания. Если идентификатор успешно получен, запрашивается описание заклинания через `spellLib.GetDescription`. Затем извлекается название заклинания. Использование логического `and` обеспечивает безопасное обращение к полям таблицы.
:::

## Смотрите также
- [spellLib.GetDescription](../spellLib/function.spellLib.GetDescription)