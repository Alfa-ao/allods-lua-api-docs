# spellLib.GetCurrentValues

Возвращает информацию о текущих значениях динамически меняющихся характеристик умения по его идентификатору.

## Описание

```lua
spellLib.GetCurrentValues( id: SpellId ): table | nil
```

Функция извлекает и возвращает таблицу с текущими значениями динамически меняющихся характеристик указанного умения. Если умение с заданным идентификатором отсутствует, возвращается `nil`.

## Список параметров

- **`id`** (`SpellId`)
Идентификатор умения.

## Возвращаемые значения

Возвращает `table` | `nil` - таблицу с информацией о текущих значениях характеристик умения, или `nil`, если такое умение отсутствует.

- **`manaCost`** (`number`) - Расход маны или энергии.

- **`prepareDuration`** (`number`) - Время подготовки в миллисекундах.

- **`range`** (`number`) - Расстояние до цели, на котором действует заклинание.

- **`minRange`** (`number`) - Минимальная дистанция до цели, на которой возможно применение умения. Если цель находится ближе, то применение невозможно.

- **`radius`** (`number`) - Радиус действия AOE заклинания от точки применения. Если заклинание не является AOE, то возвращается `0.0`.

- **`predictedCooldown`** (`number`) - Предположительная длительность кулдауна в миллисекундах.

- **`debugName`** (`string` | `nil`) - Системная информация для отладочной версии (путь к файлу). Отсутствует в FinalRelease версии клиента.

## Примеры

### Получение и вывод расхода маны умения

```lua
local spellbook = avatar.GetSpellBook()
local spellInfo = spellLib.GetCurrentValues( spellbook[1] )
common.LogInfo( spellInfo.manaCost )
```

::: info Описание примера
В примере сначала получается список умений персонажа с помощью `avatar.GetSpellBook()`. Затем для первого умения из списка запрашиваются его текущие характеристики через `spellLib.GetCurrentValues()`. После этого в лог выводится значение расхода маны.
:::

## Смотрите также

- [avatar.GetSpellBook](../avatar/function.avatar.GetSpellBook)