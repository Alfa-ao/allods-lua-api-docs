## EVENT_FPS_RANGE_CHANGED

### Описание

```
EVENT_FPS_RANGE_CHANGED
```

Событие присылается, когда изменяется диапазон FPS.

### Список параметров

- **`rangeIndex`** (`number`) - индекс диапазона, куда попадает FPS.

- **`rangeMax`** (`number`) - максимальный индекс диапазона.