---
outline: deep
---
## EVENT_GROUP_ACCEPT_ERROR

### Описание

```
EVENT_GROUP_ACCEPT_ERROR
```

Событие приходит, когда по каким-то причинам вступление в группу не сработало.

### Список параметров

Параметры отсутствуют.

---

## EVENT_GROUP_APPEARED

### Описание

```
EVENT_GROUP_APPEARED
```

Событие присылается на появление группы. Оно передается не только на действительное создание группы, но и при вхождении игрока в состав уже существующей.

### Список параметров

Параметры отсутствуют.

---

## EVENT_GROUP_CHANGED

### Описание

```
EVENT_GROUP_CHANGED
```

Событие приходит на изменение состава группы (включая создание и роспуск).

### Список параметров

Параметры отсутствуют.

---

## EVENT_GROUP_CONVERTED

### Описание

```
EVENT_GROUP_CONVERTED
```

Событие передается как уведомление о смене агрегатного состояния группы: группа -> отряд, отряд -> группа.

### Список параметров

Параметры отсутствуют.

---

## EVENT_GROUP_DECLINE

### Описание

```
EVENT_GROUP_DECLINE
```

Событие приходит при прямом отказе персонажа вступить в группу.

### Список параметров

- **`declinerName`** (`WString`) - Имя персонажа, приславшего отказ (другая информация о нем может быть недоступна).

---

## EVENT_GROUP_DECLINE_BUSY

### Описание

```
EVENT_GROUP_DECLINE_BUSY
```

Событие присылается как уведомление о невозможности пригласить персонажа в группу.

### Список параметров

- **`declinerName`** (`WString`) - Имя персонажа, от которого пришел отказ (другая информация о нем может быть недоступна).

---

## EVENT_GROUP_DECLINE_IGNORED

### Описание

```
EVENT_GROUP_DECLINE_IGNORED
```

Событие передается как уведомление о невозможности пригласить персонажа в группу по причине игнора.

### Список параметров

- **`declinerName`** (`WString`) - Имя персонажа, от которого пришел отказ.

---

## EVENT_GROUP_DISAPPEARED

### Описание

```
EVENT_GROUP_DISAPPEARED
```

Событие приходит на исчезновение группы. Оно присылается не только на роспуск группы, но и при выходе игрока из состава существующей.

### Список параметров

Параметры отсутствуют.

---

## EVENT_GROUP_INVITE

### Описание

```
EVENT_GROUP_INVITE
```

Событие присылается о приглашении в группу.

### Список параметров

- **`inviterName`** (`WString`) - Имя приглашающего (другая информация о нем может быть недоступна).
- **`inviterShardName`** (`WString`) - Имя шарда приглашающего.

---

## EVENT_GROUP_INVITE_END

### Описание

```
EVENT_GROUP_INVITE_END
```

Событие присылается в случае исчезновения приглашения в группу. Оно может истечь по времени, быть принято или отклонено.

### Список параметров

Параметры отсутствуют.

---

## EVENT_GROUP_INVITE_FAILED

### Описание

```
EVENT_GROUP_INVITE_FAILED
```

Событие приходит при невозможности пригласить игрока в группу.

### Список параметров

- **`name`** (`WString`) - Имя адресата.
- **`sysReason`** (`string`(`ENUM_GROUP_INVITE_FAILED_...`)) - Причина неудачной отправки сообщения. Значения соответствуют перечислению `ENUM_GROUP_INVITE_FAILED_...`.

---

## EVENT_GROUP_ITEM_QUALITY_CHANGED

### Описание

```
EVENT_GROUP_ITEM_QUALITY_CHANGED
```

Событие передается как уведомление о смене качества предметов для текущей схемы дележа лута в группе.

### Список параметров

Параметры отсутствуют.

---

## EVENT_GROUP_KICK_FAILED

### Описание

```
EVENT_GROUP_KICK_FAILED
```

Событие приходит при неудачной попытке выгнать игрока из группы.

### Список параметров

- **`name`** (`WString`) - Имя адресата.
- **`sysReason`** (`string`(`ENUM_GROUP_KICK_FAILED_...`)) - Причина неудачной отправки сообщения. Значения соответствуют перечислению `ENUM_GROUP_KICK_FAILED_...`.

---

## EVENT_GROUP_LEADER_CHANGED

### Описание

```
EVENT_GROUP_LEADER_CHANGED
```

Событие присылается как уведомление о смене лидера группы главного игрока.

### Список параметров

Параметры отсутствуют.

---

## EVENT_GROUP_LOOT_SCHEME_CHANGED

### Описание

```
EVENT_GROUP_LOOT_SCHEME_CHANGED
```

Событие передается как уведомление о смене схемы дележа лута в группе.

### Список параметров

Параметры отсутствуют.

---

## EVENT_GROUP_MEMBER_ADDED

### Описание

```
EVENT_GROUP_MEMBER_ADDED
```

Событие приходит при появлении нового члена группы.

### Список параметров

- **`name`** (`WString`) - Имя нового согрупника.
- **`id`** (`ObjectId` | `nil`) - Идентификатор игрока, если доступен.
- **`uniqueId`** (`UniqueId` | `nil`) - Уникальный постоянный идентификатор персонажа.

---

## EVENT_GROUP_MEMBER_CHANGED

### Описание

```
EVENT_GROUP_MEMBER_CHANGED
```

Событие приходит на изменение состояния члена группы (онлайн-офлайн, лидер).

### Список параметров

- **`name`** (`WString`) - Имя персонажа.
- **`uniqueId`** (`UniqueId` | `nil`) - Постоянный идентификатор аватара (не меняется при релогинах), чьё состояние изменилось (см. `group.GetMembers()` и `group.GetMemberInfo(id)`, поле `uniqueId` в обоих случаях); `nil`, если это наёмник-моб.
- **`id`** (`ObjectId` | `nil`) - Идентификатор члена группы; `nil`, если его нет поблизости (порвалась связь, в другой зоне и т.д.).

## Смотрите также

- [group.GetMembers](/api/category/group/function.group.GetMembers.md)
- [group.GetMemberInfo](/api/category/group/function.group.GetMemberInfo.md)

---

## EVENT_GROUP_MEMBER_REMOVED

### Описание

```
EVENT_GROUP_MEMBER_REMOVED
```

Событие приходит при выходе члена группы из ее состава.

### Список параметров

- **`name`** (`WString`) - Имя вышедшего согрупника.
- **`id`** (`ObjectId` | `nil`) - Идентификатор игрока, если доступен.
- **`uniqueId`** (`UniqueId` | `nil`) - Уникальный постоянный идентификатор персонажа.