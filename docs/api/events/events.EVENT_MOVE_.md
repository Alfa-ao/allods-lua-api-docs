## EVENT_MOVE_BY_CLICK_CHANGED

### Описание

```
EVENT_MOVE_BY_CLICK_CHANGED
```

Событие присылается каждый раз, когда изменилось состояние движения аватара в точку (начали или закончили режим движения в точку, изменилась точка назначения).

### Список параметров

Параметры отсутствуют.