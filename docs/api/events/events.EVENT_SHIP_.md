## EVENT_SHIP_CUSTOMIZING_CHANGED

### Описание

```
EVENT_SHIP_CUSTOMIZING_CHANGED
```

Событие присылается как уведомление об изменении в процессе кастомизации корабля в ангаре главного игрока.

### Список параметров

Параметры отсутствуют.

---

## EVENT_SHIP_CUSTOMIZING_FINISHED

### Описание

```
EVENT_SHIP_CUSTOMIZING_FINISHED
```

Событие присылается как уведомление о завершении кастомизации корабля в ангаре главного игрока.

### Список параметров

Параметры отсутствуют.

---

## EVENT_SHIP_CUSTOMIZING_STARTED

### Описание

```
EVENT_SHIP_CUSTOMIZING_STARTED
```

Событие присылается как уведомление о начале кастомизации корабля в ангаре главного игрока. Начиная с этого момента можно заменять визуальные украшения корабля прямо на самом корабле.

### Список параметров

Параметры отсутствуют.

---

## EVENT_SHIP_DAMAGE_RECEIVED

### Описание

```
EVENT_SHIP_DAMAGE_RECEIVED
```

Событие присылается в момент нанесения урона кораблю. Поля `attacker`, `defender`, `damageSource` могут быть равны `nil`, если за время до получения события соответствующий корабль или астральный монстр успел исчезнуть (вышел за пределы видимости, разрушился). Кроме того, `attacker` может быть равен `nil`, если стрелял не корабль и не астральный монстр. `attacker` и `defender` могут быть как кораблем, так и астральным монстром. Сторона, по которой попали, представлена в `SHIP_SIDE_...`.

### Список параметров

- **`damageTable`** (`table`) - Таблица с данными о нанесенном уроне:
    - **`attacker`** (`ObjectId` | `nil`) - Id корабля или астрального монстра, с которого стреляли.
    - **`defender`** (`ObjectId` | `nil`) - Id корабля или астрального монстра, в который попали.
    - **`damageSource`** (`ObjectId` | `nil`) - Id интерактивного объекта, который произвел выстрел (пушка).
    - **`attackerPlayer`** (`ObjectId` | `nil`) - Id игрока, который произвел выстрел.
    - **`totalDamage`** (`number`) - Общий урон, нанесенный кораблю.
    - **`hullDamage`** (`number`) - Урон, нанесенный корпусу корабля.
    - **`shieldDamage`** (`number`) - Общий урон, нанесенный по щитам.
    - **`deviceDamage`** (`number`) - Общий урон, нанесенный по другим устройствам корабля.
    - **`isCritical`** (`boolean`) - True, если нанесен критический урон.
    - **`isGlancing`** (`boolean`) - True, если нанесен уменьшенный урон.
    - **`isLethal`** (`boolean`) - True, если нанесен окончательный урон и объект погиб.
    - **`side`** (`number`(`SHIP_SIDE_...`) | `nil`) - Сторона корабля, в которую попали. Значения соответствуют перечислению `SHIP_SIDE_...`.
    - **`angle`** (`number` | `nil`) - Угол обстрела по горизонтали, для вычисления повреждения щита.
    - **`cannonPenetration`** (`number` | `nil`) - Процент пробития щита снарядом.
    - **`spellId`** (`ResourceId` | `nil`) - Id спелла, если били спеллом.
    - **`abilityId`** (`ResourceId` | `nil`) - Id абилки, если били абилкой.
    - **`buffId`** (`ResourceId` | `nil`) - Id буффа, если били буффом.
    - **`mapModifierId`** (`ResourceId` | `nil`) - Модификатор карты, если повреждения от модификатора карты.