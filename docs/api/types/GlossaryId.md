# GlossaryId

Идентификатор универсального описателя. Один из типов группы `ResourceId`. Используется для создания унифицированных описаний для произвольных сущностей.

Поля, доступные в `GlossaryId:GetInfo()`

```
name: WString - название
image: TextureId - иконка
```