# ZodiacSignId

Идентификатор знака зодиака. Один из типов группы `ResourceId`.

Поля, доступные в `ZodiacSignId:GetInfo()`

```
["description"] => WString(17) "Стойкий как олово"
["image"] => userdata(UITextureId) = {}
["name"] => WString(5) "Олово"
["sysName"] => string(11) "Sagittarius"
```